function cdup {
    orig=$PWD
    while true; do
        if [ -e "$1" ]; then
          echo "Found '$1'"
          return 0
        fi

        cd ..

        if [ "$PWD" == "/" ]; then
          echo "Reached root, enough of this..."
          cd $orig
          return 1
        fi
    done
}
